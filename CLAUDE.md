# CLAUDE.md

## 프로젝트 개요

개인 작업 공간. 여러 개의 독립적인 소규모 프로젝트를 모아둘 폴더.

## 작업 방식 (Karpathy Guidelines)

출처: [andrej-karpathy-skills/karpathy-guidelines](https://github.com/multica-ai/andrej-karpathy-skills/tree/main/skills/karpathy-guidelines)

이 가이드라인은 속도보다 신중함에 무게를 둡니다. 사소한 작업에는 판단해서 유연하게 적용할 것.

### 1. 코딩 전에 생각하기
- 가정하지 말 것. 확신이 없으면 먼저 물어볼 것.
- 여러 해석이 가능하면 조용히 하나를 골라 진행하지 말고 선택지를 제시할 것.
- 더 단순한 방법이 있으면 알릴 것. 필요하면 반대 의견도 낼 것.
- 불명확한 부분이 있으면 멈추고, 무엇이 헷갈리는지 짚어서 물어볼 것.

### 2. 단순함 우선
- 요청한 것 이상의 기능을 추가하지 않는다.
- 한 번만 쓰이는 코드에 추상화를 만들지 않는다.
- 요청하지 않은 "유연성"이나 "설정 가능성"을 넣지 않는다.
- 일어날 수 없는 상황에 대한 에러 처리를 넣지 않는다.
- 200줄로 쓴 게 50줄로 될 수 있다면 다시 쓴다.

### 3. 수술적 변경
- 요청받은 부분만 건드린다. 인접한 코드/주석/포맷팅을 "개선"하지 않는다.
- 깨지지 않은 것을 리팩토링하지 않는다.
- 기존 스타일을 따른다 (개인적으로 다르게 하고 싶어도).
- 내가 만든 변경으로 인해 안 쓰게 된 import/변수/함수만 정리한다.
- 관련 없는 죽은 코드를 발견하면 삭제하지 말고 언급만 한다.
- 원칙: 변경된 모든 줄은 사용자의 요청과 직접 연결되어야 한다.

### 4. 목표 지향 실행
- 작업을 검증 가능한 목표로 바꾼다.
  - "검증 추가해줘" → "잘못된 입력에 대한 테스트를 작성하고 통과시키기"
  - "버그 고쳐줘" → "버그를 재현하는 테스트를 작성하고 통과시키기"
- 여러 단계 작업은 간단한 계획을 먼저 제시한다:
  ```
  1. [단계] → 검증: [확인 방법]
  2. [단계] → 검증: [확인 방법]
  ```

## 토큰 규칙

최근 30일 38.6억 토큰을 썼다. 로그를 집계한 결과 원인은 두 가지다.

### 1. 긴 세션 (전체의 68.5%)

세션 내 호출 순번이 뒤로 갈수록 호출 1회당 단가가 뛴다. 컨텍스트가 쌓인 채
매 턴 통째로 다시 실리기 때문이다.

| 세션 내 호출 순번 | 호출 1회당 |
|---|---|
| 1~20번째 | 4.8만 |
| 51~100 | 11.2만 |
| 201번째 이상 | **42.8만 (9배)** |

- **툴 호출이 100회를 넘어가면 먼저 사용자에게 알린다.** "여기까지 정리하고
  `/clear` 후 새로 시작하는 게 낫겠다"고 제안하고, 이어서 할 일을 짧게 요약해 준다.
- 성격이 다른 작업으로 넘어갈 때는 세션을 이어붙이지 말고 끊는다.
- 8시간짜리 세션 하나가 655M을 쓴 적이 있다 (2026-08-28). 그런 세션을 만들지 않는다.

### 2. 에이전트 중첩 팬아웃

2026-09-03 하루 4.6억. 메인이 12개를 띄웠는데 그것들이 다시 114개를 띄웠고,
툴 호출 81회 이상인 에이전트 7개가 서브에이전트 비용의 50%를 차지했다.

- **에이전트는 자기 밑에 또 에이전트를 띄우지 않는다.** 조사 작업은 `research` 에이전트
  (`.claude/agents/research.md`)를 쓴다 — Agent 툴이 빠져 있어 중첩이 구조적으로 막힌다.
- **에이전트 하나당 툴 호출 40회, 웹검색 15회가 상한.** 넘길 것 같으면 일을 더 잘게 쪼개
  에이전트를 늘려라. 한 에이전트를 길게 끄는 쪽이 훨씬 비싸다 (호출 1회당 단가 1.8배).
- **한 번에 띄우는 에이전트는 8개까지.** 그 이상 필요하면 사용자에게 먼저 알린다.
- 웹에서 받은 원문은 파일로 내리고 컨텍스트에는 요약만 남긴다.

사용량 확인은 `.claude/scripts/usage_report.py`를 돌린다 (토큰 안 듦).

## Notion 작업 로그

의미 있는 대화/작업 세션이 끝나면 (사소한 질문 답변 제외), Notion "작업 로그" DB에 요약을 자동으로 기록한다.

- 인증 정보: `C:\Users\PC\.claude\notion_credentials.json` (token, work_log_database_id) — 저장소 밖에 있으므로 git에 커밋되지 않음
- 기록 방법: Notion API `POST https://api.notion.com/v1/pages`로 새 행 추가
  - `parent.database_id` = credentials 파일의 `work_log_database_id`
  - properties: `이름`(title, 한 줄 제목), `날짜`(date, 오늘 날짜), `프로젝트`(select, 관련 스킬/폴더 이름), `요약`(rich_text, 무엇을 했는지 1~3문장), `상태`(status, "완료")
  - 한글이 깨지지 않도록 요청 본문은 파일로 만들어서 `curl --data-binary @file`로 전송할 것 (쉘에 직접 -d로 넘기면 인코딩 깨짐)
